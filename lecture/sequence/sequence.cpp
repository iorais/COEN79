#include <iostream>
#include <assert.h>
#include "sequence.h"

sequence::sequence() {
    used = 0;
    current_index = 0;
}

void sequence::insert(value_type item) {
    assert(used < capacity);

    
}

void sequence::attatch(value_type item) {

}

void sequence::remove_current() {

}